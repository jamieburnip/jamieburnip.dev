---
date: 2020-06-05
title: "WIP - create pagination in Laravel from an array"
cover: "https://unsplash.it/1152/300/?random?BirchintheRoses"
slug: "laravel-create-pagination-from-array"
category: Code
tags:
    - php
    - laravel
    - rest api
---

Recently I was tasked with displaying information in a Laravel app using the blade 
```php
class PostController
{
    public function index(Request $request, PostApi $api)
    {
      $data = $api->allPosts();
      
      $posts = new LengthAwarePaginator(
          $items['data'],
          $items['total'],
          $items['per page'],
          $items['page'],
          ['path' => Paginator::resolveCurrentPath()]
      );
    }
```